{
  "allowedMcpPatterns": [
    { "pattern": "list", "comment": "All list operations (repos, issues, PRs, etc.)" },
    { "pattern": "get", "comment": "All get/read operations" },
    { "pattern": "search", "comment": "All search operations (code, issues, repos)" },
    { "pattern": "find", "comment": "All find operations" },
    { "pattern": "view", "comment": "All view operations" },
    { "pattern": "show", "comment": "All show operations" },
    { "pattern": "fetch", "comment": "All fetch operations" },
    { "pattern": "read", "comment": "All read operations" }
  ]
}
